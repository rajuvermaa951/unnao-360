server:
  port: 8000
  
spring: 
  datasource:
    url: jdbc:mysql://${HOST}:3306/${DB_NAME}
    username: root
    password: ${DB_PASSWORD}
    
  jpa: 
    hibernate:
      ddl-auto: validate
    show-sql: false

    
jwt: 
  key: ${JWT_KEY}
  expiration: ${JWT_EXPIRATION}